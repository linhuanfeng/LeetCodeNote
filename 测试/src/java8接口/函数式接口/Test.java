package java8接口.函数式接口;

import java.util.Comparator;

/**
 * 常用函数式接口
 *   Comparator
 * - Supplier
 * - Comsumer
 * - Predicate
 * - Function
 */
public class Test {
    public static void main(String[] args) {
//        Comparator // 属于函数式接口，且有compare和equals接口
        /*
            @FunctionalInterface
            public interface Comparator<T> {
                int compare(T o1, T o2);
                boolean equals(Object obj);
                ...
            }
         */
    }
}
